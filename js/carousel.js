"use strict";

// set each slide/image to document.querySelectorAll to HMTL class slide

// create a function

    // loop through the slides with a forEach?

    // increment slides somehow

    // use setTimeout to set how long each slide should be displayed

// call function defined above